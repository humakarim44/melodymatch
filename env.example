# Copy this file to .env and fill in your own values.
# NEVER commit the real .env file.

POSTGRES_PASSWORD=your_password_here

# Optional: only needed when running outside Docker.
# Inside Docker Compose this is set automatically to "db".
# DB_HOST=localhost
